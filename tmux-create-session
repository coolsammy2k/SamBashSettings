#!/bin/bash

session=cks

tmux new-session -s "$session"  -d "sleep 1"
sleep 0.1
~/.tmux/plugins/tpm/scripts/install_plugins.sh
for i in {0..15}; do
   tmux new-window -t "$i" -n "Q$i"
done

tmux select-window -t "$session:0"
